_schema-version: "3.2"
ID: AppGestion
version: 0.0.1
modules:
- name: appgestion-approuter
  type: approuter.nodejs
  path: appgestion-approuter
  requires:
  - name: AppGestion_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: AppGestion_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: AppGestion_html_repo_host
    parameters:
      content-target: true
resources:
- name: AppGestion_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: AppGestion_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
